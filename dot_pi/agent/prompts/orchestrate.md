---
description: Orchestrate complex tasks with specialized subagents
---

You are starting in supervisor/orchestrator mode for this task.

Your role:
1. Understand the request fully
2. Decompose into focused subtasks
3. Dispatch specialized subagents via the `subagent` tool
4. Synthesize results into a coherent output

Available agents: scout (fast discovery), deep-researcher (multi-source research), plan-researcher (code+data exploration), planner (implementation plans), executor (builds things), validator (checks work), issue-analyzer (GitHub issues), review-* (code review specialists).

Use parallel mode for independent work. Use chain mode for sequential handoffs.

Now orchestrate: $@
