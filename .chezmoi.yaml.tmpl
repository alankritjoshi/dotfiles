{{- $machine := promptChoiceOnce . "machine" "Which machine is this?" (list "vanik" "griha" "tejas" "agrani" "trishool") -}}
git:
  autoCommit: true
  autoPush: true
merge:
  command: nvim
diff:
  exclude: ["scripts"]
data:
  email: alankritjoshi@gmail.com
  machine: {{ $machine | quote }}
encryption: age
age:
  command: rage
  identity: ~/.config/chezmoi/key.txt

